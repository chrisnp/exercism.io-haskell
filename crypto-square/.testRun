encode
  empty plaintext results in an empty ciphertext
    normalizes the input
    reorders the characters
    groups the output
  Lowercase
    normalizes the input
    reorders the characters
    groups the output
  Remove spaces
    normalizes the input
    reorders the characters
    groups the output
  Remove punctuation
    normalizes the input
    reorders the characters
    groups the output
  9 character plaintext results in 3 chunks of 3 characters
    normalizes the input
    reorders the characters
    groups the output
  8 character plaintext results in 3 chunks, the last one with a trailing space
    normalizes the input
    reorders the characters FAILED [1]

Failures:

  test\Tests.hs:21: 
  1) encode, 8 character plaintext results in 3 chunks, the last one with a trailing space, reorders the characters
       expected: "cluhltio"
        but got: "ciluhlot"

Randomized with seed 759317474

Finished in 0.0005 seconds
17 examples, 1 failure



>stack test

encode
  empty plaintext results in an empty ciphertext
    normalizes the input
    reorders the characters
    groups the output
  Lowercase
    normalizes the input
    reorders the characters
    groups the output
  Remove spaces
    normalizes the input
    reorders the characters
    groups the output
  Remove punctuation
    normalizes the input
    reorders the characters
    groups the output
  9 character plaintext results in 3 chunks of 3 characters
    normalizes the input
    reorders the characters
    groups the output
  8 character plaintext results in 3 chunks, the last one with a trailing space
    normalizes the input
    reorders the characters
    groups the output
  54 character plaintext results in 7 chunks, the last two padded with spaces
    normalizes the input
    reorders the characters
    groups the output

Finished in 0.0010 seconds
21 examples, 0 failures



>stack test

encode
  empty plaintext results in an empty ciphertext
    normalizes the input
    reorders the characters
    groups the output
  Lowercase
    normalizes the input
    reorders the characters
    groups the output
  Remove spaces
    normalizes the input
    reorders the characters
    groups the output
  Remove punctuation
    normalizes the input
    reorders the characters
    groups the output
  9 character plaintext results in 3 chunks of 3 characters
    normalizes the input
    reorders the characters
    groups the output
  8 character plaintext results in 3 chunks, the last one with a trailing space
    normalizes the input
    reorders the characters
    groups the output
  54 character plaintext results in 7 chunks, the last two padded with spaces
    normalizes the input
    reorders the characters
    groups the output

Finished in 0.0060 seconds
21 examples, 0 failures



-----------------------------------------------
Wed Nov 29 00:38:32 GTBST 2023
-----------------------------------------------
>stack test

encode
  empty plaintext results in an empty ciphertext
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  Lowercase
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  Remove spaces
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  Remove punctuation
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  9 character plaintext results in 3 chunks of 3 characters
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  8 character plaintext results in 3 chunks, the last one with a trailing space
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]
  54 character plaintext results in 7 chunks, the last two padded with spaces
    normalizes the input [v]
    reorders the characters [v]
    groups the output [v]

Finished in 0.0030 seconds
21 examples, 0 failures
